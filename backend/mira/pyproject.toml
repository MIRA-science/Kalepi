[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mira"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.0"]

[dependency-groups]
dev = ["linkml>=1.8"]
