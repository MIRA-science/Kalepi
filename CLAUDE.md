# Claude Code guidance — Kalepi

Read `AGENTS.md` before doing any work in this repository. It is the source of truth for the project's goals, data model, normative rules, and v1 scope.

The canonical MIRA schema lives at `../MIRA-schema/mira.yaml`. Do not modify it directly; extend it via addition only (see R6 in AGENTS.md).
